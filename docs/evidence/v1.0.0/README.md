# Wait State 1.0.0 historical evidence

These files preserve the original public-install and real-shell render receipts
for the 1.0.0 marketplace snapshot. They do not certify current source and must
not be used as the current marketplace preview contract.

Current releases use root `.rig-proof.json`, `.render-proof.json`, and
`preview.png`. The current render receipt binds one exact direct screenshot,
clean shipment hashes, isolated Buzz run/log provenance, deterministic rig-only
PSI input, and explicit visual inspection.
